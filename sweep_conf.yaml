program: hypertune_wandb.py
method: random
metric:
  name: val_loss
  goal: minimize
parameters:
  learning_rate:
    distribution: q_log_uniform
    min: 0.0001
    max: 0.1
  # weight_decay:
  #   min:
  #   max:
  # momentum:
  #   min:
  #   max: